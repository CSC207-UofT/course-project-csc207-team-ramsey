package usecases;

import java.util.ArrayList;
import entities.User;
import entities.food.Food;

public class FoodManager {

    /**
     * adds new Food to foodList if there is no other food with the same name in the list.
     * @param name
     * @param quantity
     * @param shelfLife
     * @return boolean indicating whether food was added
     */
//    public boolean makeNewFood(User user, String name, int quantity, int shelfLife, String unit) {
//        ArrayList<Food> foodList = user.getFood();
//
//        for(int i =0; i< foodList.size(); i++){
//            if (foodList.get(i).getName().equals(name)){
//                return false;
//            }
//        }
//        Food newFood = new Food(shelfLife, name, quantity, unit);
//        user.addFood(newFood);
//        return true;
    }


//    /**
//     *
//     * @param name
//     * @param buyDate
//     * @param quantity
//     * @return
//     */
//    public boolean removeFood(User user, String name, String buyDate, int quantity){
//        ArrayList<Food> foodList = user.getFood();
//
//        for(int i =0; i< foodList.size(); i++){
//            if (foodList.get(i).getName().equals(name)){
//                user.removeFood(name, buyDate);
//            }
//        }
//    }




